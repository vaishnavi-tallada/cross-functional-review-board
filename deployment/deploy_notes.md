# Deployment Notes

## Platform
TODO: decide — Netlify / Vercel / Render (Manesh has prior experience with
Netlify Drop, so that's a good default if the app is frontend-heavy)

## Steps
1. TODO: fill in once Engineering confirms build command and env variables needed
2. TODO: environment variables required (API keys, MCP server URLs, etc.)
   — use `.env.example` as a template, never commit real keys
3. TODO: deployment URL once live

## Pre-deployment checklist
- [ ] All environment variables set
- [ ] API keys not committed to repo
- [ ] All sample proposals tested and working
- [ ] README instructions verified by running them fresh
